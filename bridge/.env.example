# Required by /v1/action auth
OPENCLAW_CLIENT_KEY=replace-with-random-secret

# Required for forwarding to OpenClaw OpenAI-compatible HTTP API
OPENCLAW_BASE_URL=https://openclaw.example.com
OPENCLAW_TOKEN=replace-with-openclaw-token
OPENCLAW_SESSION_KEY=agent:main:main
OPENCLAW_MODEL=openclaw:main

# Optional: set explicit agent routing header
# OPENCLAW_AGENT_ID=main

# Optional: if set, bridge sends completion text to Telegram via openclaw CLI
# OPENCLAW_TELEGRAM_TARGET=<chat_id_or_username>
# OPENCLAW_TELEGRAM_CHANNEL=telegram
# OPENCLAW_TELEGRAM_SEND_TIMEOUT_MS=8000
# OPENCLAW_CLI_PATH=/usr/local/bin/openclaw

# Optional forwarder tuning
OPENCLAW_FORWARD_TIMEOUT_MS=6000
OPENCLAW_FORWARD_MAX_RETRIES=1
OPENCLAW_FORWARD_DEBUG=0

# Bridge listener
BRIDGE_PORT=8787
